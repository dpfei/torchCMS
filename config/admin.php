<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 管理员密码盐
    |--------------------------------------------------------------------------
    |
    | 在 bcrypt 之前附加到明文密码上的固定盐值。bcrypt 自身已带随机盐，
    | 这里额外加的是「应用级固定盐」，作用是让同一口令在本系统内产生的
    | 哈希与其他系统完全不同，数据库泄露后无法直接拿去别处撞库。
    |
    | 安装向导会自动生成该值。**修改或清空它会导致所有已有管理员密码失效**，
    | 需要轮换时请先登录后台逐个重置密码再改。
    | 未配置时回退为基于 APP_KEY 派生，保证加盐逻辑始终成立。
    |
    */

    'password_salt' => env('ADMIN_PASSWORD_SALT', ''),

    /*
    |--------------------------------------------------------------------------
    | 登录验证码
    |--------------------------------------------------------------------------
    |
    | 后台登录表单是否展示验证码。服务器未启用 GD 扩展时自动降级为文字
    | 算术题，校验逻辑保持一致。
    |
    */

    'login_captcha' => env('ADMIN_LOGIN_CAPTCHA', true),

    /*
    | 验证码有效期（秒）
    */
    'captcha_ttl' => (int) env('ADMIN_CAPTCHA_TTL', 300),

    /*
    | 验证码字符个数
    */
    'captcha_length' => 4,

];
